dist/
.astro/
node_modules/
playwright-report/
test-results/
package-lock.json

# Byte-identical copies of the schemas shipped in the Python package. CI compares
# the two, so reformatting them here would break the "published URL == packaged
# file" guarantee.
public/schema/

# A rendered scorecard, generated by `assevra run` — not source.
public/example-scorecard.html
